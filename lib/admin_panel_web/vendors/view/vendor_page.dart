import 'package:flutter/material.dart';

class VendorPage extends StatelessWidget {
  static Page page() => const MaterialPage(child: VendorPage());
  const VendorPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
