import 'package:flutter/material.dart';

class ProductPage extends StatelessWidget {
  static Page page() => const MaterialPage(child: ProductPage());
  const ProductPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
