import 'package:flutter/material.dart';

class Orderpage extends StatelessWidget {
  static Page page() => const MaterialPage(child: Orderpage());
  const Orderpage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
