
import 'package:flutter/material.dart';

class PreviewDelegateView extends StatelessWidget {
  const PreviewDelegateView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Column(
          children: [
            
          ],
        ),
      ),
    );
  }
}