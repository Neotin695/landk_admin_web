
import 'package:flutter/material.dart';

class PreviewUserView extends StatelessWidget {
  const PreviewUserView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [],
      ),
    );
  }
}